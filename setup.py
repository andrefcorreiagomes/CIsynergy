from setuptools import setup

setup(name='cisynergy',
      version='1',
      description='conditional independence synergy measure',
      author='Andre Gomes',
      packages=['cisynergy'],
      zip_safe=False)